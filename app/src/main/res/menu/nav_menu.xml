<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        app:contentInsetStartWithNavigation = "0dp"
        android:id="@+id/nav_rating"
        android:icon="@drawable/ic_rating_mode"
        android:title="Chế độ đánh giá"
        app:actionLayout="@layout/layout_switch" />

    <item android:id="@+id/account"
        android:icon="@drawable/ic_user_avatar"
        android:title="Thông tin tài khoản"/>

    <item android:id="@+id/guideline"
        android:icon="@drawable/ic_guideline"
        android:title="Hướng dẫn"/>

    <item android:id="@+id/logout"
        android:icon="@drawable/ic_log_out"
        android:title="Đăng xuất"/>

</menu>